#pragma once

#include "include/bits/communication/topology.hpp"

namespace jada{

template<size_t N>
struct Grid{

    Grid(std::array<index_type, N> n_points, std::array<bool, N> periods)


    Topology<N> toplogy;


};


}